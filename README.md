# TMService
SNTP win32 client/server with smart time control.
Using system timer speed change for soft time correction.

File `IdSNTPX.pas` distributed under original licenses from http://www.indyproject.org/

---

## Описание

TMService — фоновое Windows-приложение (x32), которое поддерживает точность системных часов, управляя **скоростью хода** аппаратного таймера через `SetSystemTimeAdjustment`, а не просто переставляя время скачком. Дополнительно ведёт виртуальный высокоточный PFC-таймер (на основе TSC) для независимого отсчёта времени.

Архитектура:
- `TProfileTimer` (PFC-таймер) — высокоточный виртуальный таймер на счётчике TSC.
- `SetSystemTimeAdjustment` — управление скоростью системных часов Windows.
- NTP-клиент (Indy `TIdSNTP`) — синхронизация с пулом NTP-серверов.
- Встроенный SNTP-сервер — раздача времени другим хостам в сети.

---

## Конфигурационный файл

По умолчанию ищется в `c:\Apps\conf\tmservice.conf`. Формат — INI.

---

### Секция `[config]`

#### NTP-синхронизация

| Параметр | Тип | Пример | Описание |
|---|---|---|---|
| `NTPServers` | строка | `0.pool.ntp.org;time.windows.com` | Список NTP-серверов через `;`. Опрашиваются по очереди, результаты усредняются с весовым отбором по дисперсии. |
| `NTPQueryRate` | int (мин) | `1` | Начальный интервал опроса NTP в минутах. Может автоматически меняться через `AutoQueryRate`. |
| `MinQueryRate` | int (мин) | `2` | Минимальный интервал опроса (нижняя граница авторегулировки). |
| `MaxQueryRate` | int (мин) | `60` | Максимальный интервал опроса (верхняя граница авторегулировки). |
| `AutoQueryRate` | строка | `15@30,50@10,250@5` | Авторегулировка интервала NTP по текущей дивергенции PFC-таймера (мс/ч). Формат: `порог_мс/ч@интервал_мин,...`. Пример: при дивергенции < 15 мс/ч ставить интервал 30 мин, при < 50 мс/ч — 10 мин и т.д. Значения зажимаются в `[MinQueryRate, MaxQueryRate]`. |
| `VTSyncRate` | int (мин) | `1` | Интервал (в минутах) измерения расхождения системных часов vs PFC-таймер через `CompareVirtualTimer`. Рекомендуется `1`. |
| `SyncOffset` | int (мин) | `1` | Случайное смещение цикла NTP-опроса от начала часа (защита от flood при одновременном старте множества клиентов). |
| `SyncTimeout` | int (сек) | `25` | Таймаут одного NTP-запроса. |
| `TermTimeout` | int (сек) | `2` | Таймаут принудительного завершения потока синхронизации. |
| `CheckDelay` | int (мс) | `500` | Задержка перед NTP-запросом внутри цикла синхронизации. |

#### Аппаратная подстройка скорости часов

| Параметр | Тип | Пример | Описание |
|---|---|---|---|
| `HWClockAdjust` | int (мс/ч) | `1` | Начальное значение `st_adjust` — желаемое смещение скорости системных часов в **миллисекундах в час** (положительное = ускорить, отрицательное = замедлить). `0` отключает механизм аппаратной подстройки. |
| `MinClockAdjust` | int (мс/ч) | `-53000` | Нижняя граница `st_adjust`. Ограничивает максимальное замедление часов. Windows позволяет около ±1.5% от базовой частоты (~±54000 мс/ч при базовом тике 15.625 мс). |
| `MaxClockAdjust` | int (мс/ч) | `53700` | Верхняя граница `st_adjust`. |
| `HWAdjustByCPULoad` | float | `0` | Коэффициент коррекции `st_adjust` в зависимости от загрузки CPU: `st_dev = st_adjust + sta_cpu_ratio * avg_cpu_load`. Полезно если аппаратный таймер зависит от тепловыделения процессора. |
| `PWMRange` | int (мс) | `50000` | Период ШИМ-регулятора скорости часов в мс. ШИМ чередует +25 мс/ч и -25 мс/ч коррекцию для достижения дробных значений `st_adjust`. |
| `PWMSplit` | int (мс) | `25000` | Начальная позиция «разрезания» ШИМ-периода (время в фазе «ускорения»). |
| `HWClockRes` | int (100 нс) | `0` | Желаемое разрешение системного таймера Windows в единицах 100 нс (передаётся в `NtSetTimerResolution`). Меньшее значение = более частые тики = выше точность `Sleep` и временны́х меток. `0` — не изменять. Типичные значения: `156250` (15.625 мс, по умолчанию), `10000` (1 мс). |

#### PFC-таймер (виртуальный высокоточный таймер)

| Параметр | Тип | Пример | Описание |
|---|---|---|---|
| `PFCAdjust` | float (мс/ч) | `-114.326` | Поправка скорости PFC-таймера в мс/ч. Отрицательное значение — таймер немного замедлён относительно TSC (компенсирует уход кварца). Сохраняется автоматически если `PFCAdjustSave=1`. |
| `PFCAdjustSave` | bool | `1` | Автоматически сохранять скорректированное значение `PFCAdjust` в конфиг после каждой NTP-синхронизации. |
| `PFCAdjustLow` | float (мс/ч) | `-2500` | Нижняя граница автокоррекции `PFCAdjust`. |
| `PFCAdjustHigh` | float (мс/ч) | `+2500` | Верхняя граница автокоррекции `PFCAdjust`. |
| `PFCAdjustTreshold` | float (мс/ч) | `5` | Порог активации `AutoPFCAdjust` — если дивергенция PFC vs NTP меньше этого значения в мс/ч, `pfc_adjust` не трогается. |
| `AutoPFCAdjust` | bool | `1` | Включить автоматическую подстройку `PFCAdjust` по результатам NTP-синхронизации. |
| `AproxFactor` | float | `0.87` | Коэффициент аппроксимации при жёсткой коррекции системного времени: `dta := dta * AproxFactor`. Дробь < 1 означает неполную коррекцию за один шаг (остаток сглаживается в следующих циклах). |

#### Прочее

| Параметр | Тип | Пример | Описание |
|---|---|---|---|
| `BindIP` | строка | `0.0.0.0` | IP-адрес для привязки встроенного SNTP-сервера. |
| `EnableNTPServer` | bool | `0` | Включить встроенный SNTP-сервер (UDP 123). Раздаёт время другим хостам сети на основе PFC-таймера. |
| `EnableSyncStat` | bool | `0` | Логировать статистику входящих SNTP-запросов (файл `peersync*.log`). |
| `MyStratum` | int | `2` | Stratum встроенного SNTP-сервера. Обычно = stratum_источника + 1. |
| `ShowConsole` | bool | `1` | Показывать цветную консоль с лог-выводом. |
| `ShowCPUStat` | bool | `0` | Выводить в лог статистику загрузки CPU по ядрам. |
| `ProcessPriority` | int | `32` | Приоритет процесса (константы WinAPI: `32` = NORMAL, `0x8000` = ABOVE_NORMAL, `128` = HIGH). |
| `StatsFile` | строка | `c:\Apps\logs\timestats.csv` | Путь к CSV-файлу статистики синхронизаций. |
| `SaveDriftStat` | bool | `0` | Сохранять в файл `vtdrift.stat` временной ряд расхождения VTT vs системных часов. |
| `DriftLogFile` | строка | *(пусто)* | Путь к JSONL-файлу дрифт-лога. Если пустой/отсутствует — лог не ведётся. На каждую активную минуту синхронизации записывается одна JSON-строка с полями: `ts` (ISO-8601), `dta_ms`, `drift_msh`, `prv_dev_msh`, `st_adjust`, `pfc_adj_msh`, `ntp_ms`, `drift_ema_msh`, `sync_exp`, `ntp_sync`, `n_ticks`, `pwm_split`, `chk_elps_ms`. |
| `RefDtaEmaTC` | int | `2` | Постоянная времени быстрого EMA сигнала коррекции скорости (`ref_dta`). Применяется только когда `\|dta_ms\| < 1 мс` — сглаживает шум измерения в зоне точной подстройки. `0` или `1` — выключить; `2` — минимальное сглаживание (α=0.5); `5` — умеренное (α=0.2). |
| `EmaSpeedMode` | int | `0` | Режим пополнения EMA базовой скорости часов (`st_adjust_ema`): `0` — обновлять в момент экстремума дрифта (переход от роста к убыванию); `1` — обновлять только когда последние 2 измерения `\|dta_ms\|` ниже порога `EmaSpeedThr`. |
| `EmaSpeedThr` | float (мс) | `0.8` | Порог `\|dta_ms\|` для режима `EmaSpeedMode=1`. EMA обновляется только если оба последних измерения дрифта были меньше этого значения. |
| `MicroCorr` | int (мкс) | `-20` | Микрокоррекция, передаваемая в NTP-клиент (`IdSNTP1.mcs`). Компенсирует систематическую задержку обработки NTP-пакетов. |
| `PFCTrust` | float | `1.0` | Коэффициент доверия PFC-таймеру в отсутствие NTP-синхронизации. Масштабирует сигнал коррекции скорости часов (`ref_dta`) когда в текущем цикле не было опроса NTP-серверов. Диапазон: `0.0`..`1.0`. Подробнее — см. раздел ниже. |

---

#### PFCTrust — коэффициент доверия якорю

PFC-таймер (TSC-based) используется как **якорь**: именно его показания определяют, насколько системные часы спешат или отстают (`dta_ms`), и на их основе вычисляется коррекция скорости (`ref_dta → st_adjust`). Пока якорь стабилен — это работает хорошо. Но якорь бывает нестабилен:

- **Виртуальные машины** (Hyper-V, VMware, VirtualBox) — гипервизор может приостанавливать TSC при миграции vCPU между ядрами или при паузе VM. Скачки TSC выглядят как «дрифт» системных часов и вызывают ложные коррекции.
- **Троттлинг CPU** (SpeedStep, C-states) — при изменении частоты процессора счётчик TSC на части платформ некорректно масштабируется, добавляя шум ±1–5 мс/мин.
- **Многосокетные системы** — TSC разных сокетов может расходиться.

**Проблема:** если PFC-таймер сам «плывёт» на ±3 мс/мин, то измеренный `dta_ms` содержит этот шум. Алгоритм коррекции принимает его за реальный уход системных часов и начинает менять `st_adjust` в ответ на шум якоря — в результате `st_adjust` осциллирует, и реальная точность системного времени ухудшается.

**Пример с нестабильным PFC:**

```
Реальный уход системных часов:      +2 мс/ч
Шум PFC-таймера:                    ±180 мс/ч  (±3 мс/мин)
Измеренный dta_ms (система vs PFC): ±182 мс/ч  — в основном шум
Без PFCTrust: алгоритм гонится за шумом, st_adjust ±5000 единиц
С PFCTrust=0.1: коррекция ×0.1, st_adjust ±500 — менее агрессивно
```

**Значения параметра:**

| `PFCTrust` | Поведение |
|---|---|
| `1.0` | Полное доверие. Коррекция применяется в полном объёме. Оптимально когда PFC стабилен (физическая машина, modern Intel/AMD). |
| `0.5` | Половина коррекции без NTP. Хороший компромисс для умеренно нестабильного PFC (лёгкий троттлинг, некоторые VM). |
| `0.1`–`0.2` | Минимальная коррекция без NTP. Для сильно нестабильного PFC (старый Hyper-V, VirtualBox). |
| `0.0` | Коррекция скорости часов заморожена до следующего NTP-синха. Только NTP-события двигают `st_adjust`. |

Параметр действует **только когда в текущем цикле не было опроса NTP** (`ntp_sync = false`). После NTP-синхронизации коррекция всегда применяется с полным весом — в этот момент есть подтверждение от внешнего эталона.

---

### Секция `[bounds]`

Пороги, определяющие режим работы алгоритма коррекции.

| Параметр | Тип | По умолч. | Описание |
|---|---|---|---|
| `min_dvg` | float (мс) | `~16` (точность таймера) | Минимальное **абсолютное** смещение системных часов (в мс) при котором выполняется жёсткая коррекция времени (`IndySetLocalTime`). Ниже этого порога — только аппаратная подстройка скорости. |
| `max_dvg` | float (мс) | `1800000` (30 мин) | Максимальное допустимое смещение для жёсткой коррекции. Если смещение превышает это значение — коррекция считается аномальной и НЕ выполняется (требует ручного вмешательства). |
| `max_rqt` | float (мс) | `1000` | Максимальное время NTP round-trip (мс). Ответы с RQT выше этого значения отбрасываются как ненадёжные. |
| `min_date` | дата | сегодня | Минимальная допустимая дата системных часов. Если дата системы меньше — считается аномалией. |

#### Взаимодействие `min_dvg` / `max_dvg` с алгоритмом

```
|dta_ms| < min_dvg  → sync_expected = FALSE → hw_adjust (подстройка скорости),
                                               жёсткой коррекции нет
|dta_ms| in [min_dvg, max_dvg] → sync_expected = TRUE → жёсткая коррекция времени
                                                         hw_adjust НЕ выполняется (*)
|dta_ms| > max_dvg  → sync_expected = FALSE → hw_adjust, жёсткой коррекции нет
```

(*) — известное ограничение: при постоянном дрейфе > min_dvg аппаратная коррекция скорости
блокируется, и программа выполняет жёсткий сброс времени каждую минуту вместо плавной
подстройки. Уменьшение `min_dvg` (например до 50 мс) расширяет зону работы hw_adjust.

---

## Исправленные баги (история)

### v1.0.2.28+ — нестабильность st_adjust при устойчивом дрейфе

**Симптом:** при дрейфе системных часов >125 мс (> min_dvg) программа каждую минуту
выполняла жёсткий сброс времени вместо плавной подстройки скорости. Через 20–30 минут
начиналась осцилляция ±300 мс с периодом 2 минуты.

**Причина A** (`prv_dev := 0` при `sync_expected`): каждая жёсткая синхронизация сбрасывала
историю предыдущей скорости дрейфа. Следующий вызов `hw_adjust` видел `prv_dev = 0` и
удваивал коррекцию с нуля → `st_adjust` рос неограниченно → overshooting.

**Причина B** (отсутствие лимита шага): при коротком `last_chk_elps` или после `prv_dev = 0`
вычисленный `ref_dta` мог превышать 10 000 мс/ч за одну итерацию, что гарантировало
overshooting.

**Исправление A:** убран сброс `prv_dev := 0` — значение сохраняется между синхронизациями.

**Исправление B:** добавлено ограничение разового шага: `if Abs(ref_dta) > 3000 then ref_dta := Sign(ref_dta) * 3000`.
